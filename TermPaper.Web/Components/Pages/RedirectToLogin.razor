@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/LoginPage?returnUrl={Uri.EscapeDataString(Navigation.Uri)}", forceLoad: true);
    }
}
